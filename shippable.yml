# Language setting
language: python

# Version number
python:
  - "pypy3"
  
build:
  ci:
    - python --version
    - wget https://norvig.com/big.txt
    - cat big.txt  >> bigger_file.txt
    - cat big.txt >> bigger_file.txt
    - cat bigger_file.txt
